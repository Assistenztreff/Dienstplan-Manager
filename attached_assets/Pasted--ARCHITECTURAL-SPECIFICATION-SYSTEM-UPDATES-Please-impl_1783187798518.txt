### ARCHITECTURAL SPECIFICATION & SYSTEM UPDATES ###
Please implement the following business logic modifications, UI updates, and data model extensions. Apply these updates safely to the backend and frontend components.

---

#### 1. FEATURE "ABRECHNUNGSART" & ZUSCHLÄGE (IST-ZEIT LOGIK)
- Completely rebuild the surcharge calculation logic to strictly enforce "Option 2" (Calculated from Actual Clock-In Times / Ist-Zeiten).
- All night, Sunday, and holiday surcharges must be calculated dynamically based on the verified timestamps from the time tracking module ("Zeiterfassung"), NOT from the planned roster ("Dienstplan").

#### 2. DIENST- & ARBEITSZEIT-EINSTELLUNGEN (SETTINGS OVERHAUL)
- Completely remove the legacy "Arbeitszeit-Vorlagen" (Time Templates) section from the UI.
- Update the Shift Model data model ("Schichtmodelle / Dienste"). Every shift model must now include the following properties/fields in the settings:
  * Standard-Startzeit (Time)
  * Standard-Endzeit (Time)
  * Standard-Wochentage (Dropdown/Selection: "Mo-Fr", "Mo-So", or "Individuell" with checkboxes for Mon-Sun).
- Roster Integration: When an admin clicks on the calendar to add a shift, the selected shift model must auto-populate the start time, end time, and day constraints. However, the admin must still be able to manually override the times individually inside the shift creation modal.
- Bug Fix: Add a prominent "+ Neuen Dienst anlegen" (Create New Shift Model) button to the settings panel, available for both Free and Premium tiers.

#### 3. DEFAULT SYSTEM SHIFTS FOR NEW USERS (ONBOARDING)
- Upon creation/registration of a new account, the database must automatically seed the following 5 standard shift models for the user (fully editable later):
  1. Frühdienst: 08:00 – 16:00
  2. Spätdienst: 16:00 – 23:00
  3. Nachtdienst: 23:00 – 08:00 (Crosses midnight to next day)
  4. Bereitschaft: 08:00 – 14:00
  5. 24H Dienst: 09:00 – 09:00 (Crosses midnight to next day, duration: 24 hours)

#### 4. EXTENDED LOGIC FOR READY-TO-WORK SHIFTS (BEREITSCHAFTSDIENSTE)
- Add a payment type configuration ("Vergütungstyp") to the shift model settings. The admin must be able to choose between:
  * Regulärer Stundenlohn (100% base hourly rate)
  * Prozentualer Stundenlohn (Percentage field, e.g., 50% of the assistant's base hourly rate from their profile for the duration of the shift)
  * Festbetrag (Fixed flat-rate amount in EUR per shift, independent of duration).

#### 5. TIME TRACKING AUTOMATION TOGGLE (ZEITERFASSUNG)
- Add a new global configuration switch in the settings: "[ ] Stundenzettel automatisch genehmigen" (Auto-approve time entries).
- Logic:
  * If checked/active: Time entries submitted by assistants must instantly be marked as "Bestätigt" (Approved) and enter the payroll cycle.
  * If unchecked/inactive (Default): Submitted entries must remain "Offen" (Pending) until manually approved or rejected by the admin in the "Zeiterfassung" view.

#### 6. PREMIUM PAYROLL & DYNAMIC SURCHARGES (LOHNAUSWERTUNG)
- Activate financial accounting calculations inside the Premium Evaluation panel: Gross wage = (Validated Actual Hours * Assistant's base hourly rate from HR profile) adjusted by the custom ready-to-work shift rates.
- Apply dynamic surcharges based on the actual clock-in data.
- UI Filtering rule: If any surcharge rate (e.g., Sonntagszuschlag) is explicitly configured as 0% in the settings, entirely hide/omit this specific row from the final payroll generation view to ensure a clean interface.

#### 7. PREMIUM VACATION MANAGEMENT & CONVERSION LOGIC (RIGHT TO HOLIDAY)
Create a new "Urlaubsberechnungen" (Vacation Configuration) panel in settings. The system must process vacation strictly hourly in the backend but display it converted to days in the UI.

- System Basis Constants:
  * 1 Vacation Day = 8 Hours (Standard baseline value for day-to-day conversion).
  * A standard yearly allocation of 24 days translates internally to a pool of 192 vacation hours.

- Selection Method (Dropdown):
  * Method A: "Gesetzlicher 13-Wochen-Durchschnitt (§ 11 BUrlG)" (Default)
    -> When a vacation day is applied to the roster, calculate the employee's average weekly/daily hours over the trailing 3 months (13 weeks). This calculated average hour value is credited to the monthly working hour account as paid leave. The internal hour-based vacation account is reduced by this exact hour value, and divided by 8 to dynamically display the subtracted days in the UI.
  * Method B: "Prozentualer Stunden-Faktor"
    -> Scale vacation accumulation dynamically. For every confirmed working hour, credit the assistant with 0.0941 vacation hours. When vacation is taken, subtract the exact hour value of the missed rostered shift from their accumulated hourly pool.

- Deductions for 24H Shifts (Dilemma Correction Logic):
  * If a vacation entry is logged for a day containing a 24-hour shift (or fiktiv preset), credit the employee with 24 hours of paid leave. Concurrently, subtract 24 hours from the vacation pool. In the frontend UI, this must correctly display as a deduction of exactly 3.0 Vacation Days (24 hours taken / 8 hour base = 3 days).
  * If a short shift of 4 hours is taken as vacation, credit 4 hours and subtract 0.5 Vacation Days from the profile (4 hours taken / 8 hour base = 0.5 days).

---
#### 8. EMERGENCY DEPLOYMENT INSTRUCTIONS (LOCAL DATABASE ENVIRONMENT)
- To perform comprehensive integration testing on these newly defined workflows, temporarily roll back the database configuration.
- Change the active environment string to disconnect from the remote Scaleway cluster and point the application entirely back to the LOCAL REPLIT DATABASE.
- Ensure the admin account 'betreiber@dienstplan.local' is fully provisioned locally with the test rosters, assistant configurations, and last month's historical data, so I can immediately log in via the preview pane. We will migrate to Scaleway after these tests pass successfully.